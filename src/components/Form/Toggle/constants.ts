export enum ColorMode {
  light = "light",
  normal = "normal",
  dark = "dark",
}

export enum FontWeight {
  light = "light",
  normal = "normal",
  bold = "bold",
}

export enum FontSize {
  smaller = "smaller",
  normal = "normal",
  larger = "larger",
}

export enum ColorMode {
  light = "light",
  normal = "normal",
  dark = "dark",
}

export enum FontWeight {
  light = "light",
  normal = "normal",
  bold = "bold",
}

export enum FontSize {
  smaller = "14px",
  normal = "16px",
  larger = "20px",
}
